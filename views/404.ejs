<%
  const title = 'Page Not Found | RemoveWildlifeNow.com';
  const metaDesc = 'The page you were looking for could not be found.';
  const canonical = '/';
  const body = `
<section class="page-hero">
  <div class="container">
    <h1>Page Not Found</h1>
    <p>${message || 'The page you were looking for does not exist.'}</p>
    <a href="/" class="btn">Browse All States</a>
  </div>
</section>`;
%>
<%- include('layout', { title, metaDesc, canonical, body }) %>
